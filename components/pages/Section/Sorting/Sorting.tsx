import { FC } from 'react'
import classes from './Sorting.module.scss'
import { SorstingType } from './types'

const Sorting: FC<SorstingType> = ({}) => {
    return <div className={classes.Wrap}></div>
}

export default Sorting
